{
  "version": 2,
  "builds": [
  { "src": "frontend/package.json", "use": "@vercel/static-build" },
    { "src": "venv/**/*.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/venv/(.*)", "dest": "venv/main.py" },
    { "src": "/(.*)", "dest": "frontend/$1" }
  ]
}
